export interface FormValues {
  firstName: string;
  lastName: string;
  email: string;
  mobile: string;
  productId: string;
}

export interface ProductType {
  createdAt: string;
  id: string;
  name: string;
}
